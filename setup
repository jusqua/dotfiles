#!/bin/sh
set -eu

# Alises
CONFIG="$HOME/.config"

link() {
  exec rm -rf "$2/$1" &
  wait
  exec ln -nsf "$PWD/$1" $2 &
  wait
}

# Git
link .gitconfig $HOME

# Neovim
link nvim $CONFIG

# Fish
link fish $CONFIG

# Alacritty
link alacritty $CONFIG

